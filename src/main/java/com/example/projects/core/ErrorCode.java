package com.example.projects.core;

public enum ErrorCode {
    BASE_ID_NOT_FOUND,
    BASE_NOT_SAVED,
    PHONE_NUMBER_ALREADY_EXIST,
    INVALID_REQUEST,
    NO_RESULT_FOUND,
    TRIP_ALREADY_INPROGRESS;
}
